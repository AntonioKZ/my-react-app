import {useMemo,useState} from 'react'
import './survey-campaign.css'
type Person={name:string;email:string;functionName:string;role:string;status?:string;url?:string}
const parse=(raw:string)=>raw.split(/\r?\n/).map(x=>x.trim()).filter(Boolean).map(line=>{const p=line.includes(';')?line.split(';'):line.split(',');return{name:(p[0]||'').trim(),email:(p[1]||'').trim(),functionName:(p[2]||'').trim(),role:(p[3]||'').trim()}}).filter((p,i)=>i>0||!['nome','name'].includes(p.name.toLowerCase()))
export default function SurveyCampaign(){const [name,setName]=useState('Assessment aziendale 2026'),[deadline,setDeadline]=useState(''),[raw,setRaw]=useState('Nome;Email;Funzione;Ruolo\n'),[people,setPeople]=useState<Person[]>([]),[running,setRunning]=useState(false),[msg,setMsg]=useState('');const valid=useMemo(()=>people.filter(p=>p.name&&p.functionName&&p.role),[people]);const importList=()=>{const x=parse(raw);setPeople(x);setMsg(`${x.length} persone caricate. ${x.filter(p=>!p.name||!p.functionName||!p.role).length} righe incomplete.`)};const generate=async()=>{if(!valid.length)return setMsg('Carica almeno una riga valida.');setRunning(true);setMsg('Generazione campagna...');const out=[...people];let ok=0,ko=0;for(let i=0;i<out.length;i++){const p=out[i];if(!p.name||!p.functionName||!p.role){out[i]={...p,status:'DATI MANCANTI'};continue}try{const r=await fetch('/api/survey-link',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({formCode:'F03',recipientName:p.name,recipientEmail:p.email,functionName:p.functionName,recipientRole:p.role,expiresAt:deadline||null})});const j=await r.json();if(!r.ok)throw new Error();out[i]={...p,status:'CREATO',url:`${location.origin}/survey/F03/${j.token}`};ok++}catch{out[i]={...p,status:'ERRORE'};ko++}setPeople([...out])}setRunning(false);setMsg(`Campagna ${name}: ${ok} link creati, ${ko} errori.`)};const copyAll=()=>navigator.clipboard?.writeText(people.filter(p=>p.url).map(p=>`${p.name}\t${p.email}\t${p.functionName}\t${p.role}\t${p.url}`).join('\n'));const exportCsv=()=>{const csv=['Nome;Email;Funzione;Ruolo;Stato;Link',...people.map(p=>[p.name,p.email,p.functionName,p.role,p.status||'',p.url||''].map(x=>`"${String(x).replaceAll('"','""')}"`).join(';'))].join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download=`${name.replaceAll(' ','-').toLowerCase()}-inviti.csv`;a.click()};return <main className="camp"><header><div><a href="/surveys">← Questionari</a><small>SURVEY CAMPAIGN MANAGER</small><h1>Campagna F03</h1><p>Prepara in blocco gli inviti individuali per l'assessment dei dipendenti e consegna al Coverage Monitor una popolazione già classificata per funzione e ruolo.</p></div><a href="/survey-coverage">Coverage Monitor →</a></header><section className="camp-panel"><h2>1. Definisci la campagna</h2><div className="camp-grid"><label>Nome campagna<input value={name} onChange={e=>setName(e.target.value)}/></label><label>Scadenza inviti<input type="datetime-local" value={deadline} onChange={e=>setDeadline(e.target.value)}/></label></div></section><section className="camp-panel"><h2>2. Carica la popolazione</h2><p>Incolla da Excel quattro colonne nell'ordine <b>Nome; Email; Funzione; Ruolo</b>. È accettato anche CSV con virgole.</p><textarea className="bulk" value={raw} onChange={e=>setRaw(e.target.value)}/><button onClick={importList}>Importa elenco</button>{msg&&<div className="camp-msg">{msg}</div>}</section><section className="camp-panel"><div className="title"><div><h2>3. Verifica prima della generazione</h2><p>{people.length} persone · {valid.length} righe valide</p></div><button disabled={running||!valid.length} onClick={generate}>{running?'Generazione...':'Genera tutti i link F03'}</button></div><div className="table"><table><thead><tr><th>Nome</th><th>Email</th><th>Funzione</th><th>Ruolo</th><th>Stato</th><th>Link</th></tr></thead><tbody>{people.map((p,i)=><tr key={i}><td>{p.name||<b>MANCANTE</b>}</td><td>{p.email||'—'}</td><td>{p.functionName||<b>MANCANTE</b>}</td><td>{p.role||<b>MANCANTE</b>}</td><td>{p.status||'DA GENERARE'}</td><td>{p.url?<button onClick={()=>navigator.clipboard?.writeText(p.url!)}>Copia</button>:'—'}</td></tr>)}{!people.length&&<tr><td colSpan={6}>Importa prima l'elenco dei dipendenti.</td></tr>}</tbody></table></div></section>{people.some(p=>p.url)&&<section className="camp-panel"><h2>4. Distribuisci e monitora</h2><p>I link sono personali e monouso. La piattaforma non invia automaticamente email: puoi esportare la lista e distribuirla con il canale aziendale scelto.</p><div className="actions"><button onClick={copyAll}>Copia tutti i link</button><button onClick={exportCsv}>Esporta CSV inviti</button><a href="/survey-coverage">Controlla completamenti →</a></div></section>}</main>}
