import {useMemo,useState} from 'react'
import './employee-insights.css'
const read=(k:string,f:any)=>{try{return JSON.parse(localStorage.getItem(k)||'null')??f}catch{return f}}
const txt=(x:any)=>String(x||'').trim()
const count=(arr:any[],fn:(x:any)=>boolean)=>arr.filter(fn).length
export default function EmployeeInsights(){const [data]=useState<any>(()=>read('aito-store',{}));const x=useMemo(()=>{const ins=data.employeeInsights||[],tasks=(data.tasks||[]).filter((t:any)=>t.sourceSurvey),n=tasks.length||1;const signals=[['Copia / reinserimento dati',count(tasks,(t:any)=>(t.copyDetail||'')||t.data>=4)],['Attese / approvazioni',count(tasks,(t:any)=>(t.waitingDetail||'')||['often','always'].includes(t.waiting))],['Errori / rilavorazioni',count(tasks,(t:any)=>(t.errorDetail||'')||t.errors==='often')],['Alta ripetitività',count(tasks,(t:any)=>t.repeat>=5)],['Informazioni difficili da trovare',count(ins,(i:any)=>!!txt(i.hardToFind))],['Attività duplicate',count(ins,(i:any)=>!!txt(i.duplicate))],['Knowledge risk',count(ins,(i:any)=>!!txt(i.knowledgeRisk))],['Report manuali',count(ins,(i:any)=>!!txt(i.manualReport))]].map(([label,value]:any)=>({label,value,pct:Math.round(value/(label.includes('Informazioni')||label.includes('duplicate')||label.includes('Knowledge')||label.includes('Report')?(ins.length||1):n)*100)})).sort((a,b)=>b.value-a.value);const ideas=ins.flatMap((i:any)=>[['Una cosa da cambiare',i.oneChange],['Idea non richiesta',i.unaskedIdea],['Report manuale',i.manualReport],['Informazione difficile',i.hardToFind]].filter(([,v])=>txt(v)).map(([type,text])=>({type,text,who:i.respondent||'Anonimo',area:i.functionName||'n/d'})));const burden=tasks.map((t:any)=>({...t,hours:Math.round(((+t.frequency||0)*(+t.minutes||0)*(+t.people||1))/60)})).sort((a:any,b:any)=>b.hours-a.hours);return{ins,tasks,signals,ideas,burden,totalHours:burden.reduce((s:number,t:any)=>s+t.hours,0),areas:new Set(ins.map((i:any)=>i.functionName).filter(Boolean)).size}},[data]);return <main className="ei"><header><div><a href="/surveys">← Questionari</a><small>EMPLOYEE VOICE · WORK DISCOVERY</small><h1>Employee Insight Analyzer</h1><p>Trasforma le interviste F03 in una mappa aggregata delle inefficienze e delle idee proposte da chi svolge il lavoro.</p></div><a className="inbox" href="/survey-inbox">Survey Inbox →</a></header><section className="kpis"><div><b>{x.ins.length}</b><span>interviste importate</span></div><div><b>{x.tasks.length}</b><span>attività rilevate</span></div><div><b>{x.areas}</b><span>funzioni coinvolte</span></div><div><b>{x.totalHours}</b><span>h/anno osservate</span></div></section><section className="two"><div className="panel"><h2>Segnali ricorrenti</h2><p className="hint">La frequenza non dimostra da sola un problema: indica dove approfondire.</p>{x.signals.map((s:any)=><div className="signal" key={s.label}><div><b>{s.label}</b><span>{s.value} evidenze · {s.pct}%</span></div><progress max="100" value={s.pct}/></div>)}</div><div className="panel"><h2>Attività a maggiore assorbimento</h2><p className="hint">Stima basata su frequenza × durata × persone dichiarate.</p>{x.burden.slice(0,8).map((t:any)=><div className="task" key={t.id}><div><b>{t.name}</b><span>{t.tools||'strumenti n/d'}</span></div><strong>{t.hours} h/a</strong></div>)}{!x.burden.length&&<p>Nessuna attività F03 importata.</p>}</div></section><section className="panel"><h2>Voce dei dipendenti</h2><p className="hint">Le idee restano attribuite alla funzione e separate dall'interpretazione tecnica.</p><div className="ideas">{x.ideas.map((i:any,n:number)=><article key={n}><small>{i.type} · {i.area}</small><p>“{i.text}”</p><span>{i.who}</span></article>)}{!x.ideas.length&&<p>Nessun insight disponibile: importa prima le risposte F03 dalla Survey Inbox.</p>}</div></section><section className="panel"><h2>Come usare questa vista</h2><ol><li>Individua i segnali più ricorrenti.</li><li>Verifica le attività con maggiore assorbimento di tempo.</li><li>Leggi i suggerimenti senza trasformarli subito in una soluzione tecnologica.</li><li>Approfondisci con le persone coinvolte.</li><li>Porta solo le evidenze confermate nell'Opportunity Engine.</li></ol><div className="actions"><a href="/survey-inbox">Rivedi risposte</a><a href="/opportunity-engine">Opportunity Engine →</a></div></section></main>}
